# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
worktrees/
results/
queue/

# Agent prompt files (generated per-session by launchers)
CLAUDE.md
AGENTS.md

# Experimental code/artifacts
dev/

# Results file
results.tsv
*.log
local_data/
*.pid
*.npy
config_temp.json
run_result.json

# Generated experiment artifacts
wandb/
test_cache*/
# Crash-recovery outputs; keep canonical best_model.pt and results.tsv tracked/visible.
last_model.pt
best_model.pt.corrupted
results.tsv.corrupted

# One-shot architecture-diagnostic sentinels (local-only).
.auxiliary_diagnostic_done
.auxiliary_diagnostic_0p2_done
smoke_test_config.json
hard_smoke_test_config.json
reports/nnunetv2_baseline_*/
reports/lasagna_fiber_candidates/
scripts/curated_data/
scripts/_gp_winner_runner.py
scripts/_finetune_lejepa_runner.py

# Scratch / debug probes and their outputs (transient, not for the repo)
/debug_*.py
/debug_*.png
/models/
/outputs/

# Accidental local interpreter symlinks
/python
/python3

# Model Checkpoints
checkpoints/
autoresearch.out

# Discord DRAFTS stay local-only. What was actually POSTED is tracked: a file that calls
# itself "the public artifact of record" is not a record if it only exists on one machine.
# Negations must come after the pattern they exempt.
docs/DISCORD_*.md
DISCORD_*.md
!docs/DISCORD_POSTED_*.md
!DISCORD_POSTED_*.md

# Community-signal logs may include third-party handles + message text
# from Discord / GitHub; keep local-only to avoid re-publishing others'
# content without consent.
reports/community_signal_*.md
# Generated daily-report PDFs. Regenerated with an embedded timestamp, so they show up
# dirty and produce a meaningless commit every time the generator runs (the 2026-08-14
# file's entire diff was 2 bytes of timestamp). Untracked 2026-08-14; the files stay on
# disk and remain in git history if an old one is ever needed.
reports/Vesuvius_Research_Report_*.pdf
community_signal_*.md

# Skeleton-distance metric writes histogram PNGs to cwd every validation
# cycle (one per sample plus an aggregate). They get overwritten on each
# run and were polluting commits. Local-only diagnostic output.
skeleton_branch_length_hist_sample_*.png
aggregate_skeleton_branch_length_hist.png
.loop_paused
watchdog.log

# Pseudo-label self-training runtime region fragments (symlinked volumes / generated masks)
local_data/PHercParis2Fr143_Uregion/
local_data/PHercParis2Fr143_Vregion/
local_data/PHercParis2Fr143_Uregion_true/
local_data/PHercParis2Fr143/mask_Uregion.png
local_data/PHercParis2Fr143/mask_Vregion.png
experiments/pseudo_label/*.pt
experiments/pseudo_label/*.log

# Long-schedule test runtime artifacts
experiments/long_schedule/*.pt
experiments/long_schedule/*.curve.csv
experiments/long_schedule/*.log
experiments/long_schedule/cfg_smoke.json

# Overfit probe artifacts
experiments/overfit_probe/*.csv

# Aug ablation artifacts
experiments/aug_ablation/*.pt
experiments/aug_ablation/*.curve.csv
experiments/aug_ablation/*.log
experiments/aug_ablation/cfg_smoke*.json
experiments/aug_ablation/*.csv
repro/ink_segformer/runs/
villa/ink-detection/train_scrolls/
villa/ink-detection/eval_scrolls/
villa/ink-detection/.venv-gp/
villa/ink-detection/gp_weights_dl/
villa/ink-detection/timesformer_weights.ckpt
repro/gp_winner/runs/
villa/ink-detection/models/
villa/ink-detection/outputs/
